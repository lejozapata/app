# UI framework
flet==0.28.3

# Criptografía (cifrado de credenciales Gmail)
cryptography>=41.0

# Dependencias de Flet (se instalan solas, pero las fijamos para empaquetar)
httpx>=0.27
oauthlib>=3.2
repath>=0.9